name: gps_demo_tool
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  device_info_plus: ^12.4.0
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  image_picker: 1.2.0
  native_exif: 0.8.0
  path_provider: 2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
